Src:
    https://webshake.ru/symfony-course/razvorachivaem-symfony-prilozhenie-v-docker

Show:
    localhost:8092

Enter php-cli:
    docker exec -it shop-cli bash

Docker-Compose up:
    docker-compose up --build -d
    docker-compose up -d

MySQL connect for phpStorm:
    localhost
    33062
