db_data
    - хранение базы данных из контейнера

nginx
    - конф. для nginx
    - Dockerfile

php-cli
    - Dockerfile


----------------------


Show:
    localhost:8081

Enter php-cli:
    docker exec -it symfony-php-cli bash

Docker-Compose up:
    docker-compose up --build -d
    docker-compose up -d

Postgres connect for phpStorm:
    user:postgres
    localhost
    54320


